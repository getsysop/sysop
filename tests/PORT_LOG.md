# Test port log — Phase 47

Per-test classification of every gdp test ported into Sysop during
Phase 47. Mandated by spec § 3 step 14 (adversarial finding #8 — no
hand-waving "expect 5–10 tests to fail").

Classification scheme:
- **(a) script regression in Sysop** — passes in gdp, fails in Sysop because the Sysop script port introduced a bug. Fix the script; do not modify the test.
- **(b) intentional Sysop divergence** — fails because Phase 45a (or later) deliberately changed behavior. Update the test; document inline.
- **(c) gdp upstream evolution Sysop has not absorbed** — gdp added behavior in a commit later than the Sysop port. File the absorption as a separate REVIEW_CHECKLIST entry; skip with `@pytest.mark.skip(reason="awaits gdp absorption: <sha>")`.
- **(P)** — port-clean (passes in both, no adjustment needed).

## gdp baseline (run 2026-05-27)

```
cd ~/Projects/gdp-query-system && .venv/bin/python3 -m pytest \
  tests/test_next_task.py tests/test_validate_tasks.py tests/test_run_checks.py -v
```

Result: **70 passed, 0 skipped, 0 xfail.** All 70 tests were green candidates for porting.

## Per-test outcomes

### `tests/test_run_checks.py` (21 functions, ported as a single unit per spec § 3 step 11)

| # | Test | Class | Notes |
|---|---|---|---|
| 1 | `test_position_check_wrong_order_fires` | P | |
| 2 | `test_position_check_correct_order_silent` | P | |
| 3 | `test_position_check_missing_earlier_silent` | P | |
| 4 | `test_position_check_missing_later_silent` | P | |
| 5 | `test_position_check_skips_comment_lines` | P | |
| 6 | `test_eslint_parser_emits_finding_per_message` | P | |
| 7 | `test_eslint_skips_when_binary_missing` | P | |
| 8 | `test_eslint_skips_silently_when_no_frontend_dir` | b | Renamed from gdp's `test_eslint_skips_when_node_modules_missing`. Sysop's `_find_frontend_dir` returns `None` silently when no candidate exists rather than logging — the absence of any `node_modules/eslint` is normal for non-frontend projects (gdp always had `frontend/`, Sysop does not assume one). |
| 9 | `test_eslint_skips_when_eslint_config_next_missing` | P | |
| 10 | `test_eslint_empty_included_returns_no_findings` | P | |
| 11 | `test_eslint_severity_warning_mapped_to_medium` | P | |
| 12 | `test_eslint_null_rule_id_uses_syntax_error` | P | |
| 13 | `test_pip_audit_parser_emits_finding_per_vuln` | b | Diverged: gdp's fixture expected `requirements.txt:1` because the file always exists in gdp. Sysop's `_find_requirements_files` returns `[]` when no file exists; the parser falls back to `requirements.txt:1` as the anchor. The assertion still holds — the fallback path is now exercised. |
| 14 | `test_pip_audit_skips_when_binary_missing` | b | Diverged: gdp's stderr asserts `pip-audit==2.10.0` (CI-pinned version). Sysop does not own a CI workflow to pin to, so the script's warning says `pip install pip-audit` without a version. Test relaxed accordingly. |
| 15 | `test_pip_audit_empty_included_returns_no_findings` | P | |
| 16 | `test_pip_audit_clean_audit_returns_empty` | P | |
| 17 | `test_pip_audit_invalid_json_skips` | P | |
| 18 | `test_eslint_check_is_used_by_both_modes` | b | Reads `core/companion/checks.yml.fragment` instead of gdp's `.claude/checks.yml` — Sysop's fragment is the upstream source of truth; the consumer-side `.claude/checks.yml` is generated by the installer. |
| 19 | `test_pip_audit_check_is_security_audit_only` | b | Same fragment-vs-.claude rebase as #18. |
| 20 | `test_eslint_mode_filter_via_filter_checks` | b | Same fragment-vs-.claude rebase as #18. |
| 21 | `test_pip_audit_mode_filter_via_filter_checks` | b | Same fragment-vs-.claude rebase as #18. |

Sysop-original additions for the new generalization helpers and invariant boundaries live in `test_run_checks_generalizations.py` (23 functions, none ported — all spec § 3 steps 7b + 12 originals).

### `tests/test_validate_tasks.py` (8 gdp functions + 8 Sysop-original)

| # | Test | Class | Notes |
|---|---|---|---|
| 1 | `test_locks_dir_falls_back_when_not_a_git_repo` | b | Helper renamed to `_resolve_canonical_locks_dir` in Phase 32. Test name preserves the historical "locks_dir" label for searchability. |
| 2 | `test_locks_dir_falls_back_when_git_binary_missing` | b | Same rename. |
| 3 | `test_locks_dir_falls_back_on_timeout` | b | Same rename. |
| 4 | `test_locks_dir_falls_back_on_os_error` | b | Diverged behavior: Sysop catches `(FileNotFoundError, subprocess.SubprocessError)` rather than bare `OSError`. A generic `OSError`/`PermissionError` that is not a SubprocessError propagates instead of falling back — surfaces real misconfiguration. Test was rewritten to assert the propagate-on-PermissionError invariant. |
| 5 | `test_locks_dir_resolves_from_real_git_repo` | b | Helper rename only. |
| 6 | `test_locks_dir_resolves_main_locks_from_worktree` | b | Helper rename only. |
| 7 | `test_locks_dir_treats_empty_git_output_as_failure` | b | Helper rename only. |
| 8 | `test_locks_dir_resolves_inside_repo_for_staging_path` | b | Helper rename only. |
| 9 | `test_blast_radius_required_on_in_progress_at_schema_v2` | new | Sysop-original (Phase 19, commit `1bda8fc`). |
| 10 | `test_blast_radius_accepted_when_valid_value_at_v2` | new | Sysop-original (Phase 19). Note: gdp's `small` value would fail the Sysop enum (`single-file`/`single-module`/`cross-module`/`architectural`). |
| 11 | `test_blast_radius_rejects_invalid_enum_value` | new | Sysop-original (Phase 19 enum validation). |
| 12 | `test_blast_radius_optional_at_schema_v1_for_legacy_tasks` | new | Sysop-original (Phase 19 backward-compat). |
| 13 | `test_in_progress_without_lock_file_errors` | new | Sysop-original (Phase 32 / BeanRider ISSUE-0032). |
| 14 | `test_in_progress_with_lock_file_passes` | new | Sysop-original. |
| 15 | `test_done_task_with_body_under_open_dir_errors` | new | Sysop-original (Phase 18 / BeanRider ISSUE-0009 status-vs-directory drift catch). |
| 16 | `test_done_task_with_body_under_archive_dir_passes` | new | Sysop-original. |

### `tests/test_next_task.py` (41 functions ported)

All 41 gdp tests passed on first run after porting. Classifications:

| # | Test | Class | Notes |
|---|---|---|---|
| 1 | `test_load_index_round_trip` | P | |
| 2 | `test_load_index_missing_file_exits_1` | P | |
| 3 | `test_load_index_yaml_error_exits_1` | P | |
| 4 | `test_assert_schema_version_accepts_v1_and_v2` | P | |
| 5 | `test_assert_schema_version_rejects_v0` | P | |
| 6 | `test_assert_schema_version_rejects_missing` | P | |
| 7 | `test_assert_schema_version_rejects_bool` | P | |
| 8 | `test_find_focus_phase_happy_path` | P | |
| 9 | `test_find_focus_phase_zero_focus_exits_1` | P | |
| 10 | `test_find_focus_phase_two_focus_exits_1` | P | |
| 11 | `test_pick_next_task_prefers_low_effort_agent` | P | |
| 12 | `test_pick_next_task_skips_locked` | P | |
| 13 | `test_pick_next_task_skips_blocked_by_dep` | P | |
| 14 | `test_pick_next_task_skips_on_hold` | P | |
| 15 | `test_pick_next_task_returns_none_when_no_agent` | P | |
| 16 | `test_pick_next_task_filters_to_focus_phase` | P | |
| 17 | `test_pick_next_task_tie_break_by_id` | P | |
| 18 | `test_parse_review_batches_extracts_three_blocks` | P | |
| 19 | `test_parse_review_batches_captures_fields` | b | `Scope:` fixture uses placeholder vocabulary (`<api module>/server.py`) instead of gdp's `agent/server.py` per the Sysop content convention. Behavior tested is the same. |
| 20 | `test_parse_review_batches_handles_empty_input` | P | |
| 21 | `test_pick_next_batch_skips_locked_and_returns_pending` | P | |
| 22 | `test_pick_next_batch_returns_none_when_all_locked` | P | |
| 23 | `test_estimate_batch_effort_rubric` | P | |
| 24 | `test_infer_batch_user_action_detects_keyword` | b | gdp asserts `firebase` keyword. Sysop's Phase 45a port dropped gdp-specific entries (firebase, stripe dashboard, cloud sql proxy, vertex ai). Test re-anchored on `console` + `dashboard`. |
| 25 | `test_infer_batch_user_action_clean_batch` | P | |
| 26 | `test_extract_body_sections_dual_prefix` | P | |
| 27 | `test_extract_body_sections_path_containment` | P | |
| 28 | `test_extract_body_sections_user_ops` | P | |
| 29 | `test_extract_body_sections_missing_file_returns_empty` | P | |
| 30 | `test_format_task_output_shape` | P | |
| 31 | `test_format_task_output_with_user_action` | P | |
| 32 | `test_format_batch_output_shape` | P | |
| 33 | `test_format_user_action_fallback` | P | |
| 34 | `test_default_branch_prefix` | P | |
| 35 | `test_slugify_for_branch` | P | |
| 36 | `test_main_default_mode_picks_easy` | P | |
| 37 | `test_main_review_mode_no_pending` | P | |
| 38 | `test_main_review_mode_surfaces_batch` | P | |
| 39 | `test_main_default_mode_falls_back_to_batch` | P | |
| 40 | `test_main_default_mode_no_tasks` | P | |
| 41 | `test_main_aborts_on_zero_focus` | P | |

## Summary

| File | gdp ported | (P) port-clean | (a) script regression | (b) intentional divergence | (c) awaits gdp absorption | Sysop-original additions |
|---|---|---|---|---|---|---|
| `test_run_checks.py` | 21 | 14 | 0 | 7 | 0 | 23 (separate file) |
| `test_validate_tasks.py` | 8 | 0 | 0 | 8 | 0 | 8 (same file) |
| `test_next_task.py` | 41 | 39 | 0 | 2 | 0 | 0 |
| **Total** | **70** | **53** | **0** | **17** | **0** | **31** |

**Zero (a)-classified script regressions and zero (c)-classified deferrals** — all 70 gdp tests are now landed in Sysop, all (b) divergences are scoped, named, and explained inline. No follow-up REVIEW_CHECKLIST entries needed for missed gdp absorptions surfaced during this port.

## Skipped tests in gdp baseline

None. All 70 gdp tests were green candidates; none required `skip` or `xfail` carry-over.

## Files NOT covered in Phase 47

Per spec § 3.47d, these Sysop Python helpers have no gdp test counterpart and were not covered:

- `sitrep_survey.py` (1278 lines) — **covered in Phase 48** (38 tests)
- `archive_review_tasks.py` (555 lines) — **covered in Phase 48** (22 tests)
- `review_index.py` (504 lines) — **covered in Phase 48** (24 tests)
- `parse_subagent_envelope.py` (274 lines, Phase 37) — **covered in Phase 48** (23 tests)
- `permission_denied_hook.py` (206 lines, Phase 36) — **covered in Phase 48** (28 tests)
- `backfill_completed_dates.py` (221 lines) — **covered in Phase 48** (14 tests)
- `auto_claim_miner.py` + `_v2.py` (641 lines, top-level `scripts/`) — **covered in Phase 48** (21 tests)

# Phase 48 — Sysop-original test backfill

All seven helpers above gained dedicated test files. **170 new tests, 274 passing total** (was 104). No gdp counterpart for any of them, so the Phase 47 classification scheme (P / a / b / c) does not apply — every test is a Phase 48 original. The ledger here is organized by file with the public surface covered.

## Per-file outcomes

### `tests/test_parse_subagent_envelope.py` (23 functions)

Covers Phase 37's SubagentStop envelope parser end-to-end. Pure parsing helpers (`_find_envelope_block`, `_find_review_report_block`, `_extract_field`, `_parse_envelope`, `_sanitize_for_filename`) and `_main_repo_root`'s git-common-dir resolution all tested in isolation; `main()` integration covers the four documented exit paths (valid envelope → `<TASK_ID>.json`, no-envelope → `_unparseable_*.json`, bad TASK shape → `_unparseable_*.json`, empty/malformed stdin → exit 0 silent).

### `tests/test_permission_denied_hook.py` (28 functions)

Covers Phase 36's PermissionDenied hook. Three matchers (`_match_protected_push`, `_match_destructive_push`, `_match_protected_commit`) tested with both positive cases (returns guidance containing the documented `!`-escape command) and negatives (non-protected branches, non-push subjects, off-protected-branch commits). Venv-hint conditional path covered for both push matchers. `_strip_cd_prefix` covers `&&` / `;` forms + passthrough + leading-only invariant. `main()` integration covers the silent-passthrough contract for non-Bash tools, malformed stdin, and unmatched commands.

### `tests/test_backfill_completed_dates.py` (14 functions)

Covers the one-time migration helper. `_sanitize_log` covers ANSI/control-char stripping + length cap. `find_completion_date` mocks the subprocess boundary to exercise bold-form match, backtick-form fallback, no-match, and git-subprocess-error skip. `main()` integration covers: happy path (writes inferred dates back), `--dry-run` (no write), `--id-pattern` filter (subset of tasks queried), no-eligible-tasks short-circuit, missing index → exit 1, malformed YAML → exit 1.

### `tests/test_review_index.py` (24 functions)

Covers the review_tasks.md → JSON index parser. `_file_sha256` deterministic-digest contract. `parse_review_tasks` covers batch/metadata/task/deferred/grand-total recognition, one-indexed line numbers, severity + checkbox maps, trailing-batch close. `_finalize_batch` count derivation. `write_index`/`read_index` atomic round-trip + missing-file semantics. Staleness lifecycle: `is_stale` (no-index, post-rebuild, post-source-mutation), `ensure_fresh` (rebuilds-when-stale, short-circuits-when-fresh via mtime check). Query helpers (`list_batches`, `get_batch`, `get_batch_range`) cover tab-format + missing-key paths.

One adjustment during authoring: my deferred-task fixture initially placed the em-dash trailer BEFORE the emoji (`desc — reason 🟡`), which the `_DEFERRED_TASK_RE` regex treats as a single em-dash-trailer match with `severity=None`. Documented schema is emoji-first (`desc 🟡 — reason`); fixture updated to match.

### `tests/test_archive_review_tasks.py` (22 functions)

Covers the manual archival script. `_atomic_write_text` round-trip + tmp-cleanup. `parse_archivable_batches` covers: single merged batch, `Complete` status equivalence to `Merged`, mixed-status round (`all_merged` flag), `---` separator handling, trailing-batch close without `## Statistics`, `## Statistics` header as round boundary, empty result when no merged batches. `build_archive_block` covers `<details>` wrapper + separator placement between rounds + trailing-blank stripping. `build_grand_total_row` covers batch-range derivation + missing-batch-number fallback. `update_archive_total` covers total math + missing-row sentinel. `find_archive_insertion_point` covers header lookup + missing case. `update_review_tasks` covers full-round removal, partial-round single-batch removal, archive reference rewrite, deferred-TASK suffix preservation on grand-total.

### `tests/test_sitrep_survey.py` (38 functions)

Covers Phase 40's lifecycle classifier and supporting parsers. `_parse_lock_file` covers YAML happy path, non-mapping defense, YAML error, missing file. `_read_locks` covers no-dir / `.gitkeep` skip / multi-file enumeration / task_id-fallback-to-filename. `_finalize_worktree` covers main vs. non-main classification. `_read_review_batches` covers metadata extraction + missing-file passthrough. `_read_index` covers id-keying + YAML error tolerance. `_extract_doc_work_trailers` covers last-paragraph-only invariant, multi-trailer collection, case-insensitive key, malformed-ID rejection, empty body. `_is_task_shaped_branch` covers task + review prefix families and rejection of unrelated branches. `_derive_task_id_from_branch` covers explicit branch match, uppercase-suffix fallback, miss, branch-without-slash. `_phase40_fallback` covers pre-cutoff subject-match true / post-cutoff false / wrong-id false / empty-id false. `_classify_task` covers the seven real state machines: planning (no commits), ready-for-/review-close (with Doc-Work trailer), doc-work-done-unpushed, in-progress (no trailer post-cutoff), Phase-40 fallback with `~` marker, stale (lock age + no commits), branchless claim.

**Deferred to follow-up phase** (intentional — testing demands fixtures more elaborate than the tests themselves):
- `_find_discrepancies` — multi-source cross-check (locks × worktrees × index × branch list) with five distinct discrepancy kinds + branch-list `_git` shell-out.
- `_classify_review_batches` — parallel cross-check shape; depends on full subprocess + flag-truncation rendering.

One adjustment during authoring: my multi-trailer fixture initially used `FEAT-1` / `TECH-2`, both of which the script's `TASK_ID_RE` (`[A-Z0-9][A-Z0-9-]+`) rejects because it requires at least two chars after the dash. Fixture corrected to `FEAT-01` / `TECH-02`.

### `tests/test_auto_claim_miner.py` (21 functions)

Covers both `scripts/auto_claim_miner.py` and `scripts/auto_claim_miner_v2.py`, which live at top-level `scripts/` (not under `core/companion/scripts/`) because they're authoring-side retrospective miners, not workflow runtime. Smoke import + `parse_close` (single + multi-ID + whitespace tolerance + non-close subject rejection + v1/v2 parity) + `parse_iso` (standard ISO, reflog space form, trailing-Z, naive→UTC fallback, v1/v2 parity). Regex contracts (`CLAIM_RE` / `CLOSE_RE` / `DOCS_RE` / `TASK_ID_RE`) tested against canonical subject lines. v2's `classify` covers all three categories: workflow-incidental (7 canonical files), ambiguous (agents.md + `.claude/skills/` and `.claude/semgrep/` prefixes), task-content (5 representative paths).

A `sys.path.insert` in the test module surfaces top-level `scripts/` for import; the alternative — adding `scripts/` to `pyproject.toml`'s pythonpath — would shadow `core/companion/scripts/` because both contain unrelated modules.

## Summary

| File | Lines | Tests | Notes |
|---|---|---|---|
| `tests/test_parse_subagent_envelope.py` | 274 src | 23 | Phase 37 SubagentStop hook |
| `tests/test_permission_denied_hook.py` | 206 src | 28 | Phase 36 PermissionDenied hook |
| `tests/test_backfill_completed_dates.py` | 221 src | 14 | One-time migration helper |
| `tests/test_review_index.py` | 504 src | 24 | review_tasks.md → JSON parser |
| `tests/test_archive_review_tasks.py` | 555 src | 22 | Manual review archival |
| `tests/test_sitrep_survey.py` | 1278 src | 38 | Phase 40 lifecycle classifier |
| `tests/test_auto_claim_miner.py` | 641 src | 21 | v1 + v2 retrospective miners |
| **Total** | **3679 src** | **170** | + 104 prior = **274 passing** |

**Two intentional deferrals** documented above (`_find_discrepancies`, `_classify_review_batches` in sitrep_survey). Both stay testable in isolation by future-phase fixtures; current public-surface coverage of sitrep_survey is on the ~30 helpers that all other lifecycle states route through.

## Phase 61a — `tests/test_run_checks_coverage.py` (23 functions, Sysop-original)

No gdp counterpart: gdp has no coverage stage. These tests apply 58b's **test-decision discipline** — each one is recorded with what it *proves*. The stage is measurement-only and inert by default (all tools absent in CI), so the tests prove the parse + glob-filter + skip contract via the same canned-JSON-over-mocked-subprocess approach the pip-audit/eslint stage tests use (`patch("run_checks_impl.subprocess.run")`).

| Test | Proves |
|---|---|
| `test_coverage_parser_emits_finding_per_violation_line` | diff-cover `src_stats[*].violation_lines` → one `(coverage-diff-python, path:line, msg)` tuple each; msg carries the file's percent + MEDIUM |
| `test_coverage_filters_out_non_critical_path` | a file outside the `critical_path` globs is dropped; inside is kept — the crown-jewel scoping is real, not cosmetic |
| `test_coverage_clean_diff_returns_empty` | a critical-path file with no `violation_lines` → no findings |
| `test_coverage_wildcard_glob_matches_nested` | `auth/**` matches `auth/oauth/token.py` (nested) |
| `test_coverage_empty_critical_path_returns_no_findings` | no globs → no subprocess call (nothing to scope) |
| `test_coverage_skips_when_report_absent` | report file absent → no subprocess call (CI produced none) |
| `test_coverage_skips_when_binary_missing` | `FileNotFoundError` → `[]` + `pip install diff-cover` hint |
| `test_coverage_skips_on_timeout` | `TimeoutExpired` → `[]` + warning (never blocks the run) |
| `test_coverage_invalid_json_skips` | non-JSON stdout → `[]` + warning |
| `test_coverage_empty_stdout_returns_empty` | empty stdout (no diff) → `[]` |
| `test_coverage_frontend_default_report_is_lcov` | `coverage-diff-frontend` with no `report:` defaults to `coverage/lcov.info` (fed to diff-cover) |
| `test_coverage_unknown_id_without_report_skips` | an unrecognized `coverage-*` id with no `report:` → no default, skip |
| `test_run_coverage_concatenates_all_checks` | the dispatcher runs every passed check and concatenates findings across the python + frontend legs |
| `test_run_coverage_empty_list_returns_empty` | no coverage checks → `[]` (the all-tools-absent default) |
| `test_path_in_critical_directory_glob` | `billing/` matches `billing/charge.py`, `billing/sub/x.py`, and bare `billing` |
| `test_path_in_critical_bare_dir_glob` | a glob without a trailing slash (`auth`) still matches beneath it |
| `test_path_in_critical_wildcard_glob` | `api/**` and `**/pay.py` fnmatch forms both match |
| `test_path_in_critical_no_match` | unrelated paths and the `billings/` near-miss are rejected (no prefix bleed) |
| `test_path_in_critical_empty_globs` | empty glob list matches nothing |
| `test_coverage_entries_present_in_fragment` | both `coverage-*` entries exist in the core fragment with `critical_path` + `report`, and omit `paths` |
| `test_coverage_entries_are_measurement_only` | every `coverage-*` entry ships `blocking: false` (61a is measurement) — **renamed → `test_coverage_entries_are_blocking` and inverted in Phase 61b** (see below) |
| `test_coverage_check_is_codebase_review_only` | quality mode keeps coverage; security mode drops it (`used_by: [codebase-review]`) |
| `test_coverage_check_is_noop_in_grep_loop` | the pattern-less coverage entries produce nothing when passed to `run_check` (no spurious grep findings) |

**No-test gaps (recorded per 58b):** no test exercises a *real* `diff-cover` subprocess — the binary is absent in CI and the stage's value is the parse/filter/skip logic, fully covered by the canned-JSON path. A real-binary integration test is filed alongside the existing eslint/pip-audit integration follow-up (Phase 47 Medium-priority entry). +23 tests → **357 passing** (was 334).

## Phase 61b — crown-jewel hard gate + baseline carve-out (`tests/test_run_checks_coverage.py`, +10, 1 rename)

Sysop-original (no gdp counterpart). 61b arms the 61a measurement stage as a hard gate and carves coverage out of the baseline. These prove the *gate* contract on top of 61a's measurement contract. The five `main()`-level tests drive the **real** `cli.main()` (via the `_run_main` harness — non-coverage stages patched to `[]`, the `diff-cover` subprocess mocked, `--repo-root` passed so no git shell-out) so the cli emit-loop + baseline read + `--update-baseline` write are exercised for real, not stubbed. The renamed registry-contract test flips its assertion (61a: `blocking: false`; 61b: `blocking: true`).

| Test | Proves |
|---|---|
| `test_coverage_entries_are_blocking` (renamed from `…_measurement_only`) | both shipped `coverage-*` entries now ship `blocking: true` — the gate is armed in the fragment |
| `test_gate_blocks_on_uncovered_critical_line` | a blocking coverage gap fails `--fail-on-blocking` (exit 1) AND the error steers away from the dead-end `--update-baseline` path (`NOT baselineable` + `pragma`) |
| `test_gate_passes_without_fail_flag` | without `--fail-on-blocking` the gate reports the finding but exits 0 (measurement still visible) |
| `test_gate_ignores_matching_baseline` | a coverage finding with a **matching baseline key still fails** (exit 1) and is emitted live, NOT tagged `[baseline]` — the no-escape-hatch property |
| `test_measurement_only_does_not_block` | `blocking: false` keeps coverage a pure measurement — exit 0 even with `--fail-on-blocking`, finding still reported |
| `test_update_baseline_never_writes_coverage` | `--update-baseline` persists zero `coverage-` keys to the baseline file |
| `test_is_baseline_suppressed_coverage_never_suppresses` | `is_baseline_suppressed` returns `False` for a coverage id even when blocking + baselined |
| `test_is_baseline_suppressed_normal_check_still_suppresses` | the carve-out does NOT regress the normal path — a blocking + baselined non-coverage finding still suppresses |
| `test_is_baseline_suppressed_requires_blocking_and_baseline` | a non-coverage finding suppresses only when blocking AND baselined (neither alone) |
| `test_write_baseline_excludes_coverage` | `write_baseline` persists a blocking non-coverage finding but drops a coverage finding from the same call |
| `test_is_coverage_predicate` | `_is_coverage` matches the `coverage-` prefix and nothing else (`pip-audit-vuln`, grep ids excluded) |

**No-test gaps (recorded per 58b):** unchanged from 61a — no real-`diff-cover`-binary test (same filed follow-up). The new gate/baseline logic lives in `cli.py` + `baseline.py`, fully exercised in-process. +10 tests → **367 passing** (was 357).

## Phase 65b — model-sunset tooling (`tests/test_migrate_skill_model.py` + `tests/test_check_skill_models.py`)

Ported from gdp `5c25516b` (`migrate_skill_model.py` + `check_skill_models.py`). Both gdp test files port **clean** (P) — the scripts touch no DB and the tested functions are pure / `tmp_path`-driven. One shared **(b) intentional divergence** affects every test in both files: gdp's per-test `os.environ.setdefault("APP_ENV", "test")` + `_SCRIPTS = …; sys.path.insert(0, …)` boilerplate is **dropped** — Sysop's `pyproject.toml` sets `pythonpath = ["core/companion/scripts"]`, so the modules import by bare name and take no `--env` (the scripts are intentionally DB-free, like `validate_tasks.py`). gdp's `.claude/checks.yml` `argparse-without-shared-cli` exclude-list registration has **no Sysop counterpart** (that lint rule was dropped at the Phase 45a port), so nothing registers there.

### `tests/test_migrate_skill_model.py` (11 functions)

| # | Test | Class | Notes |
|---|---|---|---|
| 1 | `test_extract_pins_frontmatter_unquoted` | P | |
| 2 | `test_extract_pins_inline_both_backtick_shapes` | P | |
| 3 | `test_extract_pins_ignores_prose_word` | P | |
| 4 | `test_extract_pins_ignores_unrelated_identifiers` | P | |
| 5 | `test_migrate_frontmatter_unquoted` | P | |
| 6 | `test_migrate_inline_quoted_preserves_quote_style` | P | |
| 7 | `test_migrate_is_tier_safe_for_other_aliases` | P | the `--from`-keying guarantee: a `fable` migration leaves `sonnet`/`haiku` pins untouched |
| 8 | `test_migrate_flags_prose_without_editing` | P | |
| 9 | `test_migrate_flags_stale_rationale_line` | P | |
| 10 | `test_migrate_line_can_be_both_edited_and_flagged` | P | |
| 11 | `test_migrate_rejects_identical_from_to` | P | |

### `tests/test_check_skill_models.py` (6 gdp functions + 2 Sysop-original)

| # | Test | Class | Notes |
|---|---|---|---|
| 1 | `test_load_allowlist_skips_comments_and_blanks` | P | |
| 2 | `test_find_stale_pins_flags_unserved_alias` | P | |
| 3 | `test_find_stale_pins_clean_when_all_served` | P | |
| 4 | `test_find_stale_pins_does_not_flag_cheap_tier` | P | |
| 5 | `test_main_returns_1_on_stale_pin` | P | |
| 6 | `test_main_returns_0_when_all_served` | P | |
| 7 | `test_main_missing_allowlist_is_usage_error` | original | Sysop-added: the `--allowlist` points at a missing file → usage error (exit 2), the only `main()` path gdp's two `main` tests didn't cover |
| 8 | `test_sysop_own_skills_all_served` | original | **the CI guard the phase exists for** — asserts every pin in Sysop's own `core/skills/` is served against the shipped `core/companion/.claude/served_models.txt`; goes red in CI if a future edit introduces a stale pin or a sunset updates the allowlist without migrating the skills |

**Test decision for this phase (58b discipline):** the two scripts are pure/`tmp_path`-testable with no DB or network, so they get real unit coverage (11 + 6 ported + 2 original). The `migrate_skill_model.py --apply` write path is exercised indirectly (the `migrate_text` return tuple is the unit under test; `main()`'s write branch is a thin `path.write_text` over it). +19 tests → **386 passing** (was 367).

## Phase 69 — model roles (supersedes the Phase 65b allowlist tests; Sysop-original)

Phase 69 replaces the flat served-alias allowlist with a role-indirection layer (skills pin a `reasoning`/`mechanical`/`quick` *role*; `served_models.yml` maps role→model; the install/update resolver applies it). This has **no gdp counterpart** — it is Sysop-original. Consequences for the Phase 65b tests above:

- **`tests/test_check_skill_models.py` rewritten** (role-based). The 6 gdp-ported functions (rows 1–6) tested `load_allowlist` / `find_stale_pins`, which the role refactor removed, so they are superseded — not ported forward. The new file (8 functions) tests role resolution, the three failure modes (un-roled / undefined / sunset-unserved), local-override layering, and the renamed CI guard `test_sysop_own_skills_all_roles_served` (the role-aware successor to row 8).
- **`tests/test_migrate_skill_model.py` unchanged** — the migrator keeps its bare-alias pin primitives (now the rare role-vocabulary path); its 11 tests stay green.
- **New Sysop-original files:** `tests/test_model_roles.py` (20 — marker parse / resolve no-op + override + mixed-role + full-id + idempotence, config + local merge, the three violation modes) and `tests/test_resolve_skill_models.py` (7 — dry-run vs `--apply`, default no-op, undefined-role writes-nothing, local override, usage errors). `tests/test_install_model_roles.py` (2 — real `install.sh` ships the layer as a no-op + a pre-seeded `served_models.local.yml` resolves at install time) mirrors `test_install_concat.py`'s real-installer harness.

**Test decision:** the resolver/checker/parsers are pure or `tmp_path`/real-`install.sh`-testable with no DB or network, so they get real coverage (`_model_roles` resolution + config, the CLI exit-code contracts, and the install round-trip). +29 net → **434 passing** (was 407 at Phase 68; the 6 superseded allowlist tests removed, role-based equivalents added).
